the mic gain is accessed by pressing the Mic/Speed Proc button. That lights an led, then rotate the know for mic gain - you might need Radio Settings -> SSB MOd Source = Mic

For WSJT-X to work you need Radio Settings -> SSB MOd Source = Rear
No decodes on wsjt-x ?? need to check outside app

On app start send power on commands If the radio responds after powering on, initialization continues as normal; otherwise, it will still set the state to "radio_off".
If you get a radio_off state start at the Settings page

Change RM commands to EX

